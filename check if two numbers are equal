#include <stdio.h>

int main() {
    int m, n;

    printf("Enter the values for M and N: ");
    scanf("%d %d", &m, &n);

    if (m == n)
        printf("M and N are equal.\n");
    else
        printf("M and N are not equal.\n");

    return 0;
}
